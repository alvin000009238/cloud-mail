# Android project placeholder

This directory is reserved for the React Native Android native project (`android/`).
If initialization via React Native CLI is available, this folder should contain Gradle files and app module sources.
