# iOS project placeholder

This directory is reserved for the React Native iOS native project (`ios/`).
If initialization via React Native CLI is available, this folder should contain Xcode workspace/project files.
